spring:
  profiles:
    active: test
  application:
    name: springboot security
    description: spring boot secutiry demo .
  thymeleaf:
    cache: false
    enabled: true
    encoding: utf-8
    mode: HTML
    suffix: .html
    prefix: classpath:/templates/
  devtools:
    restart:
      enabled: true
      additional-paths: src/main/java,src/resources/templates加载
  mvc:
    view:
      prefix: classpath:/templates/
      suffix: .html
    static-path-pattern: /static/**
  data:
    mongodb:
      uri: mongodb://127.0.0.1:27017/imethan_blog_2



